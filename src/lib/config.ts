import { dev } from '$app/environment';

export const config = {
        
};

export const title = 'food for thought';
export const description = "a blog about anything i want";
export const url = dev ? 'http://localhost:5173' : 'https://blog.sjc03.org';
